# 守護獸學園 · 班級養成系統

一個為國小教師設計的「**遊戲化班級經營**」工具,把傳統的加分制升級為**虛擬寵物養成系統**——學生每次表現好,可以餵養自己的台灣特有種守護獸,從蛋孵化成神獸。

> 為什麼是台灣特有種?因為它們本身就是教材。每個孩子養著的是真實存在的、瀕危的、需要被認識的這片土地的居民。

---

## ✨ 為什麼自己做?

過去要做這樣的系統,要學 HTML、CSS、JavaScript、資料庫、後端伺服器,**至少半年起跳**。所以老師只能用現成的、忍受三個限制:

- **免費版的天花板**:學生數受限、進階功能要付費
- **無法客製化**:寵物就是那幾隻、規則就是那幾條
- **資料不是你的**:存在別人的伺服器,服務收掉就消失

這個專案示範的不只是一個系統,而是「**AI 時代,老師也可以是工具創造者**」這件事。

---

## 🎯 系統功能總覽

**核心:守護獸養成**

8 種台灣特有種(台灣黑熊、石虎、櫻花鉤吻鮭、帝雉、台灣藍鵲、穿山甲、山羌、台灣獼猴)各 4 個成長階段(蛋 → 幼獸 → 成獸 → 守護神獸)。

積分雙軌制:**累積經驗**(寵物進化用,只增不減) vs **可用積分**(商店消費用)。

健康機制:超過 3 天沒被關注的學生,寵物會「微恙」;超過 7 天會「生病」。系統會自動把這些孩子列入「需要關心」清單。

**13 個整合功能**

| 分頁 | 功能 |
|---|---|
| 老師後台 | 學生列表、寵物展示、即時積分發放 |
| 課堂工具 | 點名出席、抽籤機(可優先抽生病寵物)、計時器 |
| 聯絡簿 | 今日自動匯整、老師留言、個別備註、可列印 |
| 班級管理 | 拖曳式座位表、隨機/手動分組 |
| 商店任務 | 11 種商品(食物/配件/特殊)、班級共同任務 |
| AI 助手 | 評語生成、訊息潤飾、行為觀察分析(需自備 API Key) |
| 投影模式 | 教室大屏全班寵物展示,發分時有動畫 |
| 積分規則 | 可自訂 |
| 總覽報表 | 班級概況 + 個人學期成長報告(可列印) |
| 設定 | 班級資料、Excel 匯入、AI Key、危險區 |

---

## 🚀 快速開始

### 老師(使用者)

1. **下載整個專案**:點 GitHub 右上 `Code` → `Download ZIP`
2. **解壓縮到任意位置**(例如桌面、隨身碟)
3. **雙擊 `index.html`**——用 Chrome 或 Edge 開啟
4. 首次使用會看到設定畫面,輸入班級名稱、老師稱呼,然後:
   - 直接點「載入示範資料」看完整功能 demo
   - 或從 `assets/學生資料範本.xlsx` 編輯後匯入自己班級
   - 或手動輸入學生名單

5. **存到隨身碟、雲端硬碟、或學校電腦桌面**都可以,完全離線運作

### 老師(進階:讓 AI 幫你)

申請 Anthropic API Key:

1. 至 [console.anthropic.com](https://console.anthropic.com) 註冊
2. 充值 USD $5(約 NT$160,可使用很久)
3. 建立 API Key 並複製
4. 在系統的「**設定**」→ AI 助手區塊貼上 Key 並儲存

然後就能用三個 AI 功能:**評語生成 / 家長訊息潤飾 / 行為觀察分析**。每次使用約 NT$0.1-0.5 元。

---

## 📁 專案結構

```
guardian-classroom/
├── index.html                          # 主進入點
├── README.md                            # 你正在看的這份
├── assets/
│   ├── 學生資料範本.xlsx                  # 給老師編輯的學生匯入範本
│   └── 守護獸_Midjourney提示詞.xlsx       # 自製寵物圖的 MJ 提示詞(8 種 × 4 階段)
│
├── css/                                # 樣式表(依功能分檔)
│   ├── base.css                        # 設計變數、版面、字型
│   ├── components.css                  # 按鈕、卡片、表單、對話框
│   ├── dashboard.css                   # 老師後台
│   ├── tools.css                       # 點名/抽籤/計時器
│   ├── management.css                  # 座位表/分組
│   ├── contact.css                     # 聯絡簿/作業
│   ├── shop.css                        # 商店/任務
│   ├── ai.css                          # AI 助手
│   ├── projector.css                   # 投影模式
│   ├── report.css                      # 學期成長報表
│   └── settings.css                    # 設定/總覽
│
├── firestore.rules                     # 雲端權限規則(貼到 Firebase Console)
│
├── js/                                 # 邏輯(依功能分檔,前綴數字代表載入順序)
│   ├── 00-firebase.js                  # Firebase 設定與所有雲端操作
│   ├── 00b-session.js                  # 登入流程、身份判斷、班級切換
│   ├── 00c-auth-ui.js                  # 登入與班級選擇畫面
│   ├── 01-constants.js                 # 寵物資料、階段、預設規則
│   ├── 02-state.js                     # 全域狀態物件
│   ├── 03-storage.js                   # 儲存模組(未來換 Firebase 只改此檔)
│   ├── 04-utils.js                     # 提示訊息等工具函式
│   ├── 05-pet-engine.js                # 寵物養成核心邏輯
│   ├── 06-ai.js                        # Anthropic API 呼叫模組
│   ├── 07-excel.js                     # Excel 匯入(使用 SheetJS)
│   ├── 10-nav.js                       # 分頁切換
│   ├── feature-setup.js                # 初始設定與示範資料
│   ├── feature-students.js             # 左側學生列表
│   ├── feature-dashboard.js            # 選中學生顯示、積分發放、寵物選擇
│   ├── feature-attendance.js           # 點名出席
│   ├── feature-picker.js               # 抽籤機
│   ├── feature-timer.js                # 計時器
│   ├── feature-seating.js              # 座位表
│   ├── feature-grouping.js             # 分組(含手動模式)
│   ├── feature-contact-book.js         # 聯絡簿
│   ├── feature-homework.js             # 作業繳交追蹤
│   ├── feature-quiz.js                 # 線上測驗:出題、開放、批改、結算
│   ├── feature-student-view.js         # 學生端介面
│   ├── feature-shop.js                 # 守護獸商店
│   ├── feature-class-tasks.js          # 班級共同任務
│   ├── feature-ai-features.js          # 三項 AI 功能 + Key 管理
│   ├── feature-projector.js            # 投影模式
│   ├── feature-overview.js             # 班級總覽
│   ├── feature-report.js               # 學期成長報表
│   ├── feature-rules.js                # 積分規則編輯
│   ├── feature-settings.js             # 設定頁
│   └── 99-app.js                       # renderAll() 與啟動
```

### 怎麼修改

想改某個功能?直接打開對應的 `feature-*.js` 檔案,**不用碰其他檔案**。

幾個常見需求:

| 想改什麼 | 改哪個檔案 |
|---|---|
| 換寵物物種(不要台灣特有種,改皮卡丘?) | `js/01-constants.js` 的 `PET_SPECIES` |
| 改進化所需的積分 | `js/01-constants.js` 的 `STAGE_THRESHOLDS` |
| 新增/修改商店商品 | `js/feature-shop.js` 開頭的 `SHOP_ITEMS` |
| 改顏色主題 | `css/base.css` 的 `:root` 變數 |
| 換 AI 模型 / API 設定 | `js/06-ai.js` |
| 改進度條樣式 | `css/dashboard.css` 的 `.progress-*` |

---

## 🎨 自製寵物圖片(讓系統更有質感)

預設使用 emoji 顯示寵物(🐻 🐱 🐟...),適合先快速開始用。如果想做出有品味的版本,有兩個做法:

**做法 A:用 Midjourney 生圖**

`assets/守護獸_Midjourney提示詞.xlsx` 內含 32 個提示詞(8 種動物 × 4 階段),都已調好同物種跨階段配色一致。

1. 用 Midjourney 生成後存成 PNG
2. 命名規則:`{物種id}-{stage}.png`,例如 `bear-egg.png`、`bear-baby.png`...
3. 存到 `assets/pets/` 資料夾
4. 修改 `js/05-pet-engine.js` 的 `PetEngine.getIcon` 函式,改為回傳 `<img>` 標籤

**做法 B:用 emoji 但換主題**

例如要做寶可夢版本,只要改 `01-constants.js`:

```js
const PET_SPECIES = [
  { id: 'pika', name: '皮卡丘', icon: '⚡', desc: '電氣鼠寶可夢' },
  ...
];
```

---

## 🤖 AI 功能說明

需要自備 Anthropic API Key。三個功能:

**評語生成** — 輸入學生 + 觀察關鍵字 → 系統自動帶入該生的寵物狀態、近兩週積分、主要得分理由給 AI → 產出三種風格的評語草稿(溫暖具體 / 著重成長 / 精簡專業)。

**訊息潤飾** — 老師打草稿(可以很急很口語) + 選擇收件對象與情境 → AI 修飾成可直接寄出的版本,提供三種語氣(溫和 / 中性 / 堅定)。

**行為觀察分析** — 選學生 → 系統自動把這位學生完整的積分歷史、出席、作業繳交、商店消費等資料丟給 AI → 產出四段分析(模式 / 成因假設 / 警訊 / 建議行動)。**這是傳統工具完全做不到的——AI 看到的是時間累積的真實行為資料**。

### 費用估算

- 充值最低 USD $5
- Sonnet 4.6(推薦):一次評語約 NT$0.3,一次行為分析約 NT$0.5
- Haiku 4.5(便宜):一次評語約 NT$0.05
- 一個學期通常用不超過 USD $5

### 安全提示

API Key 只存於本機瀏覽器的 localStorage,**不會上傳任何雲端**。但若在公用電腦使用,**結束後請按「清除 API Key」**。

---

## ☁️ 雲端版(多班級 + 學生登入)

系統支援兩種運作模式,開啟時自動判斷:

| | 單機模式 | 雲端模式 |
|---|---|---|
| 資料存放 | 這台電腦的瀏覽器 | Firebase Firestore |
| 班級數 | 1 個 | 不限,頂部下拉切換 |
| 學生登入 | 不支援 | Google 帳號 |
| 線上測驗 | 不支援 | 支援,答對自動加分 |
| 費用 | 免費 | 免費(在 Firebase 免費額度內) |

設定方式見 **[docs/雲端設定指南.md](docs/雲端設定指南.md)**,約 20 分鐘。

**30 位學生同時操作為什麼不會亂?**

關鍵在寫入權責分離:班級資料只有導師寫得動,學生只能寫自己那一份作答文件
(文件 id 綁定學生帳號 uid)。因此再多人同時交卷也不會互相覆蓋。
批改在老師端進行,發布給學生的題目不含正確答案。
權限由 `firestore.rules` 強制執行,不是靠前端擋。

---

## 🔧 技術細節

- 純前端網頁,**單機模式無需後端伺服器**
- 單機模式資料存 localStorage;雲端模式存 Firestore,localStorage 當離線快取
- 使用 [SheetJS](https://sheetjs.com) 解析 Excel
- 字型使用 Google Fonts 的 Noto Serif TC + Noto Sans TC
- 設計上預留了未來接 Firebase 雲端同步的接口(只需替換 `03-storage.js`)

### 為什麼是「無框架」?

刻意不使用 React/Vue 等框架,因為:
1. 老師想客製化時不用學現代前端工具
2. 沒有 build 步驟,純 HTML 雙擊即可開啟
3. 程式碼可讀性高,適合作為教學範例
4. 體積小,網路慢也能用

---

## 📜 授權

MIT License — 自由使用、修改、分享。

但希望使用者:
- 維持「**為老師服務**」的本意,不要拿去做商業變現
- 若有改善,歡迎開 PR 回饋給原專案
- 若用於研習,可註明出處讓更多老師看到

---

## 🙏 致謝

設計靈感來自中國大陸某位四年級老師在課堂上使用的虛擬寵物積分系統,以及台灣教師社群長期討論的「**遊戲化教學**」實踐。本專案完全用 Claude(Anthropic 的 AI)協作開發,代表一個新時代的到來——**老師也能造工具**。

每隻守護獸都是台灣特有種,牠們是這片土地上比我們先住的居民。希望孩子們在養守護獸的過程中,記得這份共處。
